<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/libs/bootstrap/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/static/css/style.css">

    <title><%= title %> - Bologna</title>
</head>
<body class="bg-light">
    <%- include('../partials/nav') %>
    <div class="container mt-3">
        <% if (typeof message != "undefined") { %>
            <div class="alert alert-<%= message.class %>">
                <%= message.text %> 
            </div>
        <% } %>
        <div class="row">
            
            <div class="col-md-6 alt mb-5 mt-4 ">
            <form action="" method="post">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <div class="row mb-3 mt-3">
                        <label for="tc" class="col-sm-4 col-form-label">TC kimlik no</label>
                        <div class="col-sm-8">
                            <input type="number" class="form-control" id="tc" name="tc">
                        </div>
                    </div>
                    <div class="row mb-3 mt-3">
                        <label for="password" class="col-sm-4 col-form-label">Parola</label>
                        <div class="col-sm-8">
                            <input type="password" class="form-control" id="password" name="password">
                        </div>
                    </div>
                    <div class="row mb-3 text-center mt-4">
                        <div class="col-sm-10 offset-sm-2">
                          <button type="submit" class="btn btn-primary">Oturum Aç</button>
                        </div>
                    </div>
                </form>
            </div>
            <div class="col-md-6 text-center ust mb-5 mt-3">
                <img src="/static/images/logo.png" alt="" class="img-fluid box-shadoww">
            </div>
          
        </div>
    </div>
</body>
</html>